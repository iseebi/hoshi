import styled from "@emotion/styled";
import React from "react";

type Props = object;

const Frame = styled.div``;

const StartView: React.FC<Props> = () => <Frame>StartView</Frame>;

export default StartView;
